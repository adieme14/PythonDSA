# Fast Lookup, Counting, or Grouping

### A hash table is a good fit when the problem needs one of these:
* Finding pairs with a specific sum
* Check whether a value has been seen before
* Count frequencies
* Checking for duplicates
* Map a key to an index or object
* Group values by a normalized key
* Implementing caches or memoization